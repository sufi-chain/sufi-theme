name: Publish NuGet Packages to nuget.org

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      package_version:
        description: "SemVer package version (required for manual runs). Example: 1.2.3 or 1.2.3-beta.1"
        type: string
        required: true

permissions:
  contents: read
  id-token: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_VERSION: "10.0.x"
  NUGET_ORG_SOURCE_URL: https://api.nuget.org/v3/index.json
  NUGET_PUSH_PARALLELISM: "4"
  PACKAGE_OUTPUT: ./artifacts/nuget
  PACKAGE_PROJECTS: >-
    src/SufiChain.SufiTheme/SufiChain.SufiTheme.csproj
    src/SufiChain.SufiTheme.Blazor/SufiChain.SufiTheme.Blazor.csproj
    src/SufiChain.SufiTheme.Blazor.Server/SufiChain.SufiTheme.Blazor.Server.csproj
    src/SufiChain.SufiTheme.Blazor.WebAssembly.Bundling/SufiChain.SufiTheme.Blazor.WebAssembly.Bundling.csproj
    src/SufiChain.SufiTheme.Blazor.WebAssembly/SufiChain.SufiTheme.Blazor.WebAssembly.csproj
  EXPECTED_PACKAGE_IDS: >-
    SufiChain.SufiTheme
    SufiChain.SufiTheme.Blazor
    SufiChain.SufiTheme.Blazor.Server
    SufiChain.SufiTheme.Blazor.WebAssembly.Bundling
    SufiChain.SufiTheme.Blazor.WebAssembly

concurrency:
  group: publish-nuget-org-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: nuget-org
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Write CI NuGet.config
        shell: bash
        run: |
          set -euo pipefail
          cat > NuGet.Config <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <packageSources>
              <clear />
              <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
            </packageSources>
          </configuration>
          EOF
          # Strip leading YAML indentation from the generated config.
          sed -i 's/^[[:space:]]*//' NuGet.Config

      - name: Resolve package version
        id: version
        shell: bash
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_PACKAGE_VERSION: ${{ inputs.package_version }}
        run: |
          set -euo pipefail
          if [ -n "${RELEASE_TAG:-}" ]; then
            version="$RELEASE_TAG"
          elif [ -n "${INPUT_PACKAGE_VERSION:-}" ]; then
            version="$INPUT_PACKAGE_VERSION"
          else
            echo "Package version is required." >&2
            exit 1
          fi
          version="${version#v}"
          if ! printf '%s' "$version" | grep -Eq '^[0-9]+([.][0-9]+){2}(-[0-9A-Za-z][0-9A-Za-z.-]*)?([+][0-9A-Za-z][0-9A-Za-z.-]*)?$'; then
            echo "Invalid NuGet package version: $version" >&2
            echo "Use SemVer like 1.2.3, 1.2.3-beta.1, or v1.2.3." >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $version"

      - name: Restore, build and pack packages
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          package_dir="${PACKAGE_OUTPUT%/}/${VERSION}"
          rm -rf "$package_dir"
          mkdir -p "$package_dir"

          for project in $PACKAGE_PROJECTS; do
            if [ ! -f "$project" ]; then
              echo "Package project does not exist: $project" >&2
              exit 1
            fi
            echo "Packing $project"
            dotnet restore "$project" \
              --configfile NuGet.Config \
              --verbosity minimal \
              -p:NuGetAudit=false \
              -p:UseSufiProjectReferences=false
            dotnet build "$project" \
              --configuration Release \
              --no-restore \
              --verbosity minimal \
              -m \
              -p:PackageVersion="$VERSION" \
              -p:ContinuousIntegrationBuild=true \
              -p:BuildInParallel=true \
              -p:UseSufiProjectReferences=false \
              -p:PackageOutputPath="$(pwd)/$package_dir" \
              -p:GeneratePackageOnBuild=true
          done

          find "$package_dir" -type f -name '*.nupkg' ! -name '*.symbols.nupkg' ! -name "*.${VERSION}.nupkg" -exec rm -f {} +
          expected_count=0
          for package_id in $EXPECTED_PACKAGE_IDS; do
            expected_count=$((expected_count + 1))
            package_path="$package_dir/${package_id}.${VERSION}.nupkg"
            if [ ! -f "$package_path" ]; then
              echo "Expected package was not produced: $package_path" >&2
              exit 1
            fi
          done
          package_count="$(find "$package_dir" -maxdepth 1 -type f -name "*.${VERSION}.nupkg" ! -name '*.symbols.nupkg' | wc -l | tr -d ' ')"
          if [ "$package_count" != "$expected_count" ]; then
            echo "Expected exactly $expected_count package(s), but found $package_count in $package_dir." >&2
            exit 1
          fi
          echo "Packed all $package_count expected package(s) for version $VERSION into $package_dir."
          find "$package_dir" -type f -name "*.${VERSION}.nupkg" ! -name '*.symbols.nupkg' | sort

      - name: Log in to nuget.org with Trusted Publishing
        id: nuget-login
        uses: NuGet/login@v1
        with:
          user: ${{ vars.NUGET_USER }}

      - name: Push packages to nuget.org
        shell: bash
        env:
          NUGET_ORG_API_KEY: ${{ steps.nuget-login.outputs.NUGET_API_KEY }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          if [ -z "${NUGET_ORG_API_KEY:-}" ]; then
            echo "NuGet Trusted Publishing did not return a temporary API key." >&2
            exit 1
          fi
          package_dir="${PACKAGE_OUTPUT%/}/${VERSION}"
          mapfile -t packages < <(find "$package_dir" -type f -name "*.${VERSION}.nupkg" ! -name '*.symbols.nupkg' | sort)
          if [ ${#packages[@]} -eq 0 ]; then
            echo "No NuGet packages found for version $VERSION." >&2
            exit 1
          fi
          echo "Pushing ${#packages[@]} package(s) for version $VERSION to nuget.org."
          printf '%s\0' "${packages[@]}" | xargs -0 -n 1 -P "$NUGET_PUSH_PARALLELISM" sh -c '
            package="$1"
            for attempt in 1 2 3; do
              if dotnet nuget push "$package" \
                --source "$NUGET_ORG_SOURCE_URL" \
                --api-key "$NUGET_ORG_API_KEY" \
                --skip-duplicate; then
                exit 0
              fi
              echo "Push failed for $package (attempt $attempt/3)." >&2
              sleep $((attempt * 5))
            done
            exit 1
          ' sh
