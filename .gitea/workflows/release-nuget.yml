name: Release NuGet Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      package_version:
        description: Optional SemVer package version. Defaults to a timestamped dev prerelease.
        type: string
        required: false

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  NUGET_ORG_SOURCE_URL: https://api.nuget.org/v3/index.json
  PACKAGE_OUTPUT: ./artifacts/nuget
  PACKAGE_PROJECTS: src/SufiChain.SufiTheme/SufiChain.SufiTheme.csproj src/SufiChain.SufiTheme.Blazor/SufiChain.SufiTheme.Blazor.csproj src/SufiChain.SufiTheme.Blazor.Server/SufiChain.SufiTheme.Blazor.Server.csproj src/SufiChain.SufiTheme.Blazor.WebAssembly.Bundling/SufiChain.SufiTheme.Blazor.WebAssembly.Bundling.csproj src/SufiChain.SufiTheme.Blazor.WebAssembly/SufiChain.SufiTheme.Blazor.WebAssembly.csproj

concurrency:
  group: release-nuget-${{ gitea.ref_name }}
  cancel-in-progress: false

jobs:
  pack-and-push:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/dotnet/sdk:10.0
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve package version
        id: version
        shell: sh
        env:
          RELEASE_TAG: ${{ gitea.event.release.tag_name }}
          INPUT_PACKAGE_VERSION: ${{ inputs.package_version }}
        run: |
          set -eu
          if [ -n "${RELEASE_TAG:-}" ]; then
            version="$RELEASE_TAG"
          elif [ -n "${INPUT_PACKAGE_VERSION:-}" ]; then
            version="$INPUT_PACKAGE_VERSION"
          else
            version="0.0.0-dev.$(date +%Y%m%d%H%M%S)"
          fi
          version="${version#v}"
          if ! printf '%s' "$version" | grep -Eq '^[0-9]+([.][0-9]+){2}(-[0-9A-Za-z][0-9A-Za-z.-]*)?([+][0-9A-Za-z][0-9A-Za-z.-]*)?$'; then
            echo "Invalid NuGet package version: $version" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Restore, build and pack packages
        shell: sh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -eu
          package_dir="${PACKAGE_OUTPUT%/}/${VERSION}"
          rm -rf "$package_dir"
          mkdir -p "$package_dir"
          for project in $PACKAGE_PROJECTS; do
            dotnet restore "$project" \
              --source "$NUGET_ORG_SOURCE_URL" \
              --verbosity minimal \
              -p:NuGetAudit=false
            dotnet build "$project" \
              --configuration Release \
              --no-restore \
              --verbosity minimal \
              -p:PackageVersion="$VERSION" \
              -p:ContinuousIntegrationBuild=true
            dotnet pack "$project" \
              --configuration Release \
              --no-build \
              --output "$package_dir" \
              --verbosity minimal \
              -p:PackageVersion="$VERSION" \
              -p:ContinuousIntegrationBuild=true
          done

      - name: Push packages to nuget.org
        shell: sh
        env:
          NUGET_ORG_API_KEY: ${{ secrets.NUGET_ORG_API_KEY }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -eu
          if [ -z "${NUGET_ORG_API_KEY:-}" ]; then
            echo "NUGET_ORG_API_KEY secret is required." >&2
            exit 1
          fi
          find "${PACKAGE_OUTPUT%/}/${VERSION}" -type f -name '*.nupkg' ! -name '*.symbols.nupkg' -print0 | \
            xargs -0 -n 1 dotnet nuget push \
              --source "$NUGET_ORG_SOURCE_URL" \
              --api-key "$NUGET_ORG_API_KEY" \
              --skip-duplicate
